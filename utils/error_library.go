package utils

var (
	ErrorFormatIDStr     = "id must be an integer and can't be empty"
	ErrorFormatLimitStr  = "limit must be an integer"
	ErrorFormatOffsetStr = "limit must be an integer"
	ErrorFormatSlugStr   = "slug format is wrong"
	ErrorFormatCatIDStr  = "category_id must be an integer and can't be empty"

	ErrorTitleEmptyStr       = "title can't be empty"
	ErrorMetadataEmptyStr    = "Metadata can't be empty"
	ErrorFHTMLContenEmptyStr = "html_content can't be empty"
)
